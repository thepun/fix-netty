apply plugin: 'idea'

idea {
    module {
        excludeDirs += file('.build')

        downloadJavadoc = true
        downloadSources = true

        inheritOutputDirs = false
        outputDir = file("${buildDir}/idea/out")
        testOutputDir = file("${buildDir}/idea/test_out")
    }
}